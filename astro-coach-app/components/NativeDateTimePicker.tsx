/**
 * Native-only date/time picker — on web, `NativeDateTimePicker.web.tsx` is used so Metro never bundles the community module.
 */
export { default } from "@react-native-community/datetimepicker";
