{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "npx serve . --listen tcp://0.0.0.0:$PORT --single",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
